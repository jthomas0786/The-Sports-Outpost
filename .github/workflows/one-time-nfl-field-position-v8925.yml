name: One-time NFL field position authority v89.25

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/one-time-nfl-field-position-v8925.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Repair live NFL field position authority
        run: |
          python3 - <<'PY'
          from pathlib import Path
          import re

          def replace_once(path, old, new, label):
              p=Path(path); s=p.read_text()
              if new in s:
                  print(label, 'already applied'); return
              if old not in s:
                  raise SystemExit(f'missing anchor: {label}')
              p.write_text(s.replace(old,new,1))
              print('applied', label)

          # Browser authority: recover the current spot from ESPN territory text
          # when the low-latency feed does not provide yardFromOwn.
          p=Path('sports/nfl/live.js'); s=p.read_text()
          s=s.replace("downDistanceText=sit.shortDownDistanceText||sit.downDistanceText||sit.possessionText||null;",
                      "downDistanceText=sit.downDistanceText||sit.shortDownDistanceText||sit.possessionText||null;")
          old="""    const bl=sit.ballLocation;
    if(bl&&finite(bl.yardLine)!=null&&bl.team?.id!=null&&possId!=null){const own=String(bl.team.id)===String(possId);yardFromOwn=own?Number(bl.yardLine):100-Number(bl.yardLine);if(!own)isRedZone=yardFromOwn>=80;}
  }
  return {status:state,statusDetail:type.shortDetail||(type.completed?type.shortDetail:null),period:status.period||0,clockMin:parseClock(status.displayClock||status.shortClock),awayScore:away.score!=null?Number(away.score):null,homeScore:home.score!=null?Number(home.score):null,possession,yardFromOwn,isRedZone,down,distance,downDistanceText,lastPlayText,lastFetchedAt:Date.now()};"""
          new="""    const bl=sit.ballLocation;
    if(bl&&finite(bl.yardLine)!=null&&bl.team?.id!=null&&possId!=null){const own=String(bl.team.id)===String(possId);yardFromOwn=own?Number(bl.yardLine):100-Number(bl.yardLine);if(!own)isRedZone=yardFromOwn>=80;}
    if(yardFromOwn==null&&possession){
      const txt=String(sit.possessionText||sit.downDistanceText||'').toUpperCase();
      const m=txt.match(/\\b([A-Z]{2,3})\\s+(\\d{1,2})\\b/);
      const possAbbr=teamNorm(possession==='home'?home.team?.abbreviation:away.team?.abbreviation);
      const oppAbbr=teamNorm(possession==='home'?away.team?.abbreviation:home.team?.abbreviation);
      if(m){const loc=teamNorm(m[1]),yard=Number(m[2]);if(loc===possAbbr)yardFromOwn=yard;else if(loc===oppAbbr)yardFromOwn=100-yard;}
      else if(/\\b50\\b/.test(txt))yardFromOwn=50;
      if(yardFromOwn!=null)isRedZone=yardFromOwn>=80;
    }
  }
  return {status:state,statusDetail:type.shortDetail||(type.completed?type.shortDetail:null),period:status.period||0,clockMin:parseClock(status.displayClock||status.shortClock),awayScore:away.score!=null?Number(away.score):null,homeScore:home.score!=null?Number(home.score):null,possession,yardFromOwn,isRedZone,down,distance,downDistanceText,possessionText:sit?.possessionText||null,lastPlayText,lastFetchedAt:Date.now()};"""
          if old not in s and new not in s: raise SystemExit('missing browser extractLive field-position anchor')
          s=s.replace(old,new,1)

          old="function exactOwnYard(yardsToEndzone){const n=finite(yardsToEndzone);return n==null?null:clamp(100-n,0,100);}\n"
          new="""function exactOwnYard(yardsToEndzone){const n=finite(yardsToEndzone);return n==null?null:clamp(100-n,0,100);}
function yardFromTerritoryText(value,g,side){
  if(side!=='away'&&side!=='home')return null;
  const txt=String(value||'').toUpperCase();if(!txt)return null;
  const poss=teamForSide(g,side),opp=teamForSide(g,side==='away'?'home':'away');
  const to=txt.match(/\\bTO\\s+([A-Z]{2,3})\\s+(\\d{1,2})\\b/);
  const all=[...txt.matchAll(/\\b([A-Z]{2,3})\\s+(\\d{1,2})\\b/g)];
  const m=to||all.at(-1);
  if(m){const team=teamNorm(m[1]),yard=Number(m[2]);if(Number.isFinite(yard)&&yard>=0&&yard<=50){if(team===poss)return yard;if(team===opp)return 100-yard;}}
  if(/\\b50\\b/.test(txt))return 50;
  return null;
}
function resolveCurrentOwn(gl,g,possession,currentPlay){
  const raw=finite(gl?.yardFromOwn);if(raw!=null)return clamp(raw,0,100);
  for(const value of [gl?.possessionText,gl?.downDistanceText,gl?.currentDrive?.endText,currentPlay?.description,gl?.lastPlayText]){
    const y=yardFromTerritoryText(value,g,possession);if(y!=null)return y;
  }
  const prior=_acceptedByGame.get(String(g?.gameId||g?.id||''));
  if(prior?.possession===possession&&finite(prior?.yardFromOwn)!=null)return clamp(Number(prior.yardFromOwn),0,100);
  return null;
}
"""
          if old not in s and new not in s: raise SystemExit('missing live resolver insertion anchor')
          s=s.replace(old,new,1)
          s=s.replace("let currentOwn=finite(gl?.yardFromOwn);if(currentOwn!=null)currentOwn=clamp(currentOwn,0,100);",
                      "let currentOwn=resolveCurrentOwn(gl,g,possession,currentPlay);")
          s=s.replace("function rememberAccepted(g,gl){_acceptedByGame.set(String(g?.gameId||g?.id||''),{status:gl?.status||g?.status||'',period:Number(gl?.period)||0,clockMin:Number.isFinite(Number(gl?.clockMin))?Number(gl.clockMin):null,awayScore:Number.isFinite(Number(gl?.awayScore))?Number(gl.awayScore):Number(g?.away?.score)||0,homeScore:Number.isFinite(Number(gl?.homeScore))?Number(gl.homeScore):Number(g?.home?.score)||0,acceptedAt:Date.now()});}",
                      "function rememberAccepted(g,gl,possession,gamecastState){_acceptedByGame.set(String(g?.gameId||g?.id||''),{status:gl?.status||g?.status||'',period:Number(gl?.period)||0,clockMin:Number.isFinite(Number(gl?.clockMin))?Number(gl.clockMin):null,awayScore:Number.isFinite(Number(gl?.awayScore))?Number(gl.awayScore):Number(g?.away?.score)||0,homeScore:Number.isFinite(Number(gl?.homeScore))?Number(gl.homeScore):Number(g?.home?.score)||0,possession,yardFromOwn:finite(gamecastState?.currentYardFromOwn),acceptedAt:Date.now()});}")
          s=s.replace("g.liveScore={period:gl.period,clockMin:gl.clockMin,possession,yardFromOwn:gl.yardFromOwn,isRedZone:gl.isRedZone,down:gl.down,distance:gl.distance,downDistanceText:gl.downDistanceText,lastPlayText:gl.lastPlayText,currentPlay,gamecastState,winProbability:gl.winProbability||null,linescores:gl.linescores||null,currentDrive:gl.currentDrive||null,plays:Array.isArray(gl.plays)?gl.plays:[],playerStats:gl.playerStats||null,boxScore:gl.boxScore||null,teamStats:gl.teamStats||null,scoringPlays:Array.isArray(gl.scoringPlays)?gl.scoringPlays:[],lastFetchedAt:gl.lastFetchedAt||data.lastFetchedAt,source};",
                      "g.liveScore={period:gl.period,clockMin:gl.clockMin,possession,yardFromOwn:gamecastState?.currentYardFromOwn??gl.yardFromOwn,isRedZone:gl.isRedZone,down:gl.down,distance:gl.distance,downDistanceText:gl.downDistanceText,possessionText:gl.possessionText||null,lastPlayText:gl.lastPlayText,currentPlay,gamecastState,winProbability:gl.winProbability||null,linescores:gl.linescores||null,currentDrive:gl.currentDrive||null,plays:Array.isArray(gl.plays)?gl.plays:[],playerStats:gl.playerStats||null,boxScore:gl.boxScore||null,teamStats:gl.teamStats||null,scoringPlays:Array.isArray(gl.scoringPlays)?gl.scoringPlays:[],lastFetchedAt:gl.lastFetchedAt||data.lastFetchedAt,source};")
          s=s.replace("rememberAccepted(g,gl);patchClockOnly(g,gl,possession);","rememberAccepted(g,gl,possession,gamecastState);patchClockOnly(g,gl,possession);")
          s=s.replace("export const __LIVE_TEST__={playYards,playKind,playDirection,currentPlayFrom,buildGamecastState,candidatePlayers,findPlayerInText,participantPlayer,exactOwnYard,exactPlayEndFromOriginalOffense};",
                      "export const __LIVE_TEST__={playYards,playKind,playDirection,currentPlayFrom,buildGamecastState,candidatePlayers,findPlayerInText,participantPlayer,exactOwnYard,exactPlayEndFromOriginalOffense,yardFromTerritoryText,resolveCurrentOwn};")
          p.write_text(s)

          # Supabase authority: ESPN now exposes the live territory primarily as
          # possessionText/downDistanceText (e.g. IND 4), not ballLocation.
          p=Path('supabase/functions/nfl-live/index.ts'); s=p.read_text()
          helper="""
function yardFromTerritoryText(text: unknown, possessionAbbr: unknown, opponentAbbr: unknown) {
  const txt = String(text ?? "").toUpperCase();
  const m = txt.match(/\\b([A-Z]{2,3})\\s+(\\d{1,2})\\b/);
  if (m) {
    const team = norm(m[1]);
    const yard = Number(m[2]);
    if (Number.isFinite(yard) && yard >= 0 && yard <= 50) {
      if (team === norm(possessionAbbr)) return yard;
      if (team === norm(opponentAbbr)) return 100 - yard;
    }
  }
  if (/\\b50\\b/.test(txt)) return 50;
  return null;
}
"""
          anchor="async function json(url: string) {"
          if helper.strip() not in s:
              if anchor not in s: raise SystemExit('missing Supabase helper anchor')
              s=s.replace(anchor,helper+'\n'+anchor,1)
          old="""  if (bl && Number.isFinite(Number(bl.yardLine)) && bl.team?.id != null && possId) {
    const own = String(bl.team.id) === possId;
    yardFromOwn = own ? Number(bl.yardLine) : 100 - Number(bl.yardLine);
    if (!own) isRedZone = yardFromOwn >= 80;
  }

  return {"""
          new="""  if (bl && Number.isFinite(Number(bl.yardLine)) && bl.team?.id != null && possId) {
    const own = String(bl.team.id) === possId;
    yardFromOwn = own ? Number(bl.yardLine) : 100 - Number(bl.yardLine);
    if (!own) isRedZone = yardFromOwn >= 80;
  }
  if (yardFromOwn == null && possession) {
    const possessionAbbr = possession === "home" ? h.team?.abbreviation : a.team?.abbreviation;
    const opponentAbbr = possession === "home" ? a.team?.abbreviation : h.team?.abbreviation;
    yardFromOwn = yardFromTerritoryText(sit.possessionText || sit.downDistanceText, possessionAbbr, opponentAbbr);
    if (yardFromOwn != null) isRedZone = yardFromOwn >= 80;
  }

  return {"""
          if old not in s and new not in s: raise SystemExit('missing Supabase ball location anchor')
          s=s.replace(old,new,1)
          s=s.replace("      sit.shortDownDistanceText || sit.downDistanceText || sit.possessionText || null,",
                      "      sit.downDistanceText || sit.shortDownDistanceText || sit.possessionText || null,",1)
          s=s.replace("    lastPlayText: sit.lastPlay?.text || sit.lastPlay?.shortText || null,",
                      "    possessionText: sit.possessionText || null,\n    lastPlayText: sit.lastPlay?.text || sit.lastPlay?.shortText || null,",1)
          s=s.replace('schemaVersion: 5,','schemaVersion: 6,')
          p.write_text(s)

          # Keep the alternate worker/static builder on the same ESPN mapping.
          p=Path('workers/nfl-live-worker.js'); s=p.read_text()
          if 'function yardFromTerritoryText(' not in s:
              s=s.replace("async function json(url){", "function yardFromTerritoryText(text,poss,opp){const txt=String(text||'').toUpperCase(),m=txt.match(/\\b([A-Z]{2,3})\\s+(\\d{1,2})\\b/);if(m){const team=norm(m[1]),yard=Number(m[2]);if(Number.isFinite(yard)&&yard>=0&&yard<=50){if(team===norm(poss))return yard;if(team===norm(opp))return 100-yard;}}return /\\b50\\b/.test(txt)?50:null;}\nasync function json(url){",1)
          s=s.replace("  if(bl&&Number.isFinite(Number(bl.yardLine))&&bl.team?.id!=null&&possId){\n    const own=String(bl.team.id)===possId; yardFromOwn=own?Number(bl.yardLine):100-Number(bl.yardLine); if(!own)isRedZone=yardFromOwn>=80;\n  }",
                      "  if(bl&&Number.isFinite(Number(bl.yardLine))&&bl.team?.id!=null&&possId){\n    const own=String(bl.team.id)===possId; yardFromOwn=own?Number(bl.yardLine):100-Number(bl.yardLine); if(!own)isRedZone=yardFromOwn>=80;\n  }\n  if(yardFromOwn==null&&possession){const poss=possession==='home'?h.team?.abbreviation:a.team?.abbreviation,opp=possession==='home'?a.team?.abbreviation:h.team?.abbreviation;yardFromOwn=yardFromTerritoryText(sit.possessionText||sit.downDistanceText,poss,opp);if(yardFromOwn!=null)isRedZone=yardFromOwn>=80;}",1)
          s=s.replace("downDistanceText:sit.shortDownDistanceText||sit.downDistanceText||sit.possessionText||null,lastPlayText:sit.lastPlay?.text||null,",
                      "downDistanceText:sit.downDistanceText||sit.shortDownDistanceText||sit.possessionText||null,possessionText:sit.possessionText||null,lastPlayText:sit.lastPlay?.text||null,",1)
          p.write_text(s)

          p=Path('scripts/nfl-live-poller.mjs'); s=p.read_text()
          if 'function yardFromTerritoryText(' not in s:
              s=s.replace("async function getJson(url){", "function yardFromTerritoryText(text,poss,opp){const txt=String(text||'').toUpperCase(),m=txt.match(/\\b([A-Z]{2,3})\\s+(\\d{1,2})\\b/);if(m){const team=normTeam(m[1]),yard=Number(m[2]);if(Number.isFinite(yard)&&yard>=0&&yard<=50){if(team===normTeam(poss))return yard;if(team===normTeam(opp))return 100-yard;}}return /\\b50\\b/.test(txt)?50:null;}\nasync function getJson(url){",1)
          s=s.replace("  if(bl&&Number.isFinite(Number(bl.yardLine))&&bl.team?.id!=null&&possId){\n    const own=String(bl.team.id)===possId; yardFromOwn=own?Number(bl.yardLine):100-Number(bl.yardLine); if(!own)isRedZone=yardFromOwn>=80;\n  }",
                      "  if(bl&&Number.isFinite(Number(bl.yardLine))&&bl.team?.id!=null&&possId){\n    const own=String(bl.team.id)===possId; yardFromOwn=own?Number(bl.yardLine):100-Number(bl.yardLine); if(!own)isRedZone=yardFromOwn>=80;\n  }\n  if(yardFromOwn==null&&possession){const poss=possession==='home'?teamAbbr(home):teamAbbr(away),opp=possession==='home'?teamAbbr(away):teamAbbr(home);yardFromOwn=yardFromTerritoryText(sit.possessionText||sit.downDistanceText,poss,opp);if(yardFromOwn!=null)isRedZone=yardFromOwn>=80;}",1)
          s=s.replace("downDistanceText:sit.shortDownDistanceText||sit.downDistanceText||sit.possessionText||null,\n    lastPlayText:sit.lastPlay?.text||sit.lastPlay?.shortText||null,",
                      "downDistanceText:sit.downDistanceText||sit.shortDownDistanceText||sit.possessionText||null,\n    possessionText:sit.possessionText||null,\n    lastPlayText:sit.lastPlay?.text||sit.lastPlay?.shortText||null,",1)
          p.write_text(s)

          # Cache-bust the live poller all the way to index.html.
          replace_once('sports/nfl-preview.js',"./nfl/live.js?v=89.21","./nfl/live.js?v=89.22",'base live import')
          p=Path('sports/nfl-preview-v890.js'); s=p.read_text(); s=s.replace("./nfl/live.js?v=89.21","./nfl/live.js?v=89.22").replace("./nfl-preview.js?v=89.20.4","./nfl-preview.js?v=89.20.5"); p.write_text(s)
          replace_once('sports/nfl-preview-v893.js',"./nfl-preview-v890.js?v=89.20.5","./nfl-preview-v890.js?v=89.20.6",'v890 wrapper cache')
          replace_once('sports/router.js',"./nfl-preview-v893.js?v=89.29","./nfl-preview-v893.js?v=89.30",'NFL wrapper cache')
          p=Path('index.html'); s=p.read_text(); s2=re.sub(r'\.\/sports\/router\.js\?v=[A-Za-z0-9._-]+','./sports/router.js?v=90.27',s,count=1)
          if s2==s and './sports/router.js?v=90.27' not in s: raise SystemExit('missing index router cache anchor')
          p.write_text(s2)
          PY
      - name: Validate field position resolver
        run: |
          node --check sports/nfl/live.js
          node --check sports/nfl-preview.js
          node --check sports/nfl-preview-v890.js
          node --check sports/nfl-preview-v893.js
          node --check sports/router.js
          node --check workers/nfl-live-worker.js
          node --check scripts/nfl-live-poller.mjs
          node --input-type=module - <<'NODE'
          import {__LIVE_TEST__ as T} from './sports/nfl/live.js';
          const game={gameId:'field-test',away:{abbr:'BAL'},home:{abbr:'IND'}};
          if(T.yardFromTerritoryText('2nd & Goal at IND 4',game,'away')!==96)throw new Error('opponent territory did not map to BAL own-96');
          if(T.yardFromTerritoryText('1st & 10 at BAL 38',game,'away')!==38)throw new Error('own territory did not map to BAL own-38');
          if(T.yardFromTerritoryText('1st & 10 at 50',game,'away')!==50)throw new Error('midfield did not map to 50');
          const gl={possessionText:'IND 4',downDistanceText:'2nd & Goal',lastPlayText:'D.Henry right tackle to IND 4 for 2 yards'};
          if(T.resolveCurrentOwn(gl,game,'away',{description:gl.lastPlayText})!==96)throw new Error('current live spot resolver failed');
          console.log('NFL field position resolver: BAL at IND 4 => own 96; own-side and midfield mappings pass');
          NODE
          grep -F 'yardFromTerritoryText' supabase/functions/nfl-live/index.ts
          grep -F 'possessionText: sit.possessionText || null' supabase/functions/nfl-live/index.ts
          grep -F 'schemaVersion: 6' supabase/functions/nfl-live/index.ts
          grep -F "./nfl/live.js?v=89.22" sports/nfl-preview.js
          grep -F "./nfl-preview-v893.js?v=89.30" sports/router.js
          grep -F './sports/router.js?v=90.27' index.html
          node scripts/nfl-v8916-live-personnel-selftest.mjs
          node scripts/nfl-v8915-gamecast-motion-selftest.mjs
      - name: Commit field position fix
        run: |
          git config user.name 'TSO NFL Bot'
          git config user.email 'actions@users.noreply.github.com'
          git add sports/nfl/live.js sports/nfl-preview.js sports/nfl-preview-v890.js sports/nfl-preview-v893.js sports/router.js index.html supabase/functions/nfl-live/index.ts workers/nfl-live-worker.js scripts/nfl-live-poller.mjs
          if git diff --cached --quiet; then exit 0; fi
          git commit -m 'Fix NFL Gamecast field position tracking'
          git pull --rebase origin main
          git push origin HEAD:main
